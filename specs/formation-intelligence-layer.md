# Formation Intelligence Layer (6MDM / closure spine) — usable synthesis

Codename: Steve Austin / Six Million Dollar Man. "We have the technology" — the organs are built;
this is the SPINE that connects them. Per §4 (anti-ghost): this is an ASSEMBLER/OVERLAY over existing
engines, NOT a new detection subsystem. Do not rebuild detection.

## The one genuinely-new primitive

**Formation lifecycle state** — a first-class object tracking ONE structural pattern's maturity over time:

```
LATENT → EMERGING → STRUCTURING → CRYSTALLIZING → ESTABLISHED → DISSOLVING
```

Distinct from the convergence classifier: convergence scores a signal FIELD per query; a formation
state tracks a single pattern's maturity across time. This object does not exist yet — it is the build.
Rule: show MATURITY, not certainty (consistent with statecontract.js PROJECTION, DEF-1863).

## Formation integrity checks → already-built engines (assemble, don't rebuild)

| Check | Existing component |
|-------|--------------------|
| Persistence (structural vs temporary) | convergence hysteresis buffer + confirmationvelocity.js |
| Cross-domain coherence (independent alignment) | causalimpactmap.js + shared-pool router (§16) |
| Evidence diversity (not one amplified source) | evidenceweighting.js (KRYL-977) + SCI coveredTypes/classCoverage |
| Competing explanations (survives "what else?") | RFA loop (abduce→close→deduce→heal) + historicaldivergence.js |
| Crystallization threshold | convergence states + HP qualification floor |

## Formation drivers (the "why") → EDGE

Universal drivers: constraint · resource imbalance · incentive change · information asymmetry ·
network effects · phase transition. These are the EDGE asymmetry mechanisms (KRYL-1044:
LIQUIDITY_IMBALANCE, SUPPLY_CONSTRAINT, OWNERSHIP_CONCENTRATION, TIMING_WINDOW, OPTION_CONVEXITY,
INFORMATION_ASYMMETRY). Universal driver → domain translation → observed signals → formation.

## The Brief becomes a Formation Brief (re-sequence existing outputs, no new analysis)

Report follows formation order, each section = an existing engine output:

1. Formation Overview (what pattern is becoming visible) — BLUF + formation state
2. Signal Field (ingredients, not conclusions) — signal pool / node surface
3. Relationship Fabric (the intelligence is the relationship) — causalMap typed edges (KRYL-1050) + causal impact map
4. Formation State (lifecycle, avoids premature certainty) — the NEW primitive
5. Formation Drivers (why it's forming) — EDGE (KRYL-1044)
6. Counter-Formation (what would invalidate it) — RFA competing-explanation survivors
7. Crystallization Indicators (what would broaden awareness — observation, not prediction) — confirmationvelocity
8. Decision Boundary (stops BEFORE recommendation) — HP floor + statecontract (never "act")

Terminology: prefer "Formation Brief" over "report" — the artifact reinforces the mental model.

## Decision-theory grounding (validates the split already shipped)

- **Dual-process** → System 1 = lens surfaces (fast perception) · System 2 = Brief + evidence ledger (deliberate).
- **Fuzzy-Trace "gist"** → BLUF + formation state (compressed structural representation, kept traceable).
- **Decision Field Theory threshold** → crystallization state / HP qualification floor.
- **Bounded rationality** → KRYLO's job is attention scarcity, not information scarcity: reveal structure.
- **Theory-Based View** → the causal/RFA discipline: observe → hypothesize → test → update → preserve uncertainty.

Principle: a decision is triggered not by information volume but by a change in the internal
representation of reality. KRYLO makes that coherence visible while preserving the evidence chain
that prevents FALSE coherence.

## The build (two overlays, not new engines)

1. **Formation lifecycle state object** — the maturity ladder above the existing engines.
2. **Formation Brief assembler** — orders existing engine outputs into formation sequence.

Everything else is already institutionalized (Grounded-or-Withhold, §22 absence, §21 route-don't-aggregate,
EDGE invariants). The rebuild is the spine, not the limbs.
